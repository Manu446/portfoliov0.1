import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  root: './',              // project root
  publicDir: 'public',     // points to the root public folder
  build: {
    outDir: 'dist',        // Vercel serves this folder
    emptyOutDir: true
  }
});
